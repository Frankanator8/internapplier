{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "grade_interview_output",
  "description": "Output of the grade_interview agent. Exactly these two top-level keys in this order: score, feedback.",
  "type": "object",
  "additionalProperties": false,
  "required": ["score", "feedback"],
  "properties": {
    "score": {
      "type": "number",
      "minimum": 0,
      "maximum": 10,
      "description": "Number 0-10, decimals allowed."
    },
    "feedback": {
      "type": "string",
      "description": "Specific, actionable feedback string consumed by the candidate to revise their response. May be empty (\"\") if the response is strong enough that there is nothing actionable to add."
    }
  }
}
